export default function Orders() {
  return (
    <div>
      <h1>Orders Page</h1>
      <p>This is the orders page for your account.</p>
      {/* include order history and order tracking here, so that this page is entirely about the order logic. */}
    </div>
  );
}
